export class TEntidad {
    constructor() {
        // Constructor
    }

    // Dibujar la entidad
    dibujar(matriz) {
        console.warn('El método dibujar debe ser implementado por la subclase');
    }
}
